# HKDL Conqueror 2026 — APKビルド手順

## 📦 プロジェクト概要
- **アプリ名**: HKDL Conqueror 2026
- **パッケージ名**: com.hkdl.conqueror2026
- **最小SDK**: 26 (Android 8.0+)
- **Kotlin**: 2.1.0 / Jetpack Compose / Material3

---

## 🚀 Android Studio でのビルド手順（5分）

### 1. 前提条件
- Android Studio Ladybug (2024.2.1) 以降をインストール済み
- JDK 17 以上（Android Studio 同梱のJDK可）

### 2. プロジェクトを開く
1. このZIPを解凍し、`HKDLConqueror2026` フォルダを取得
2. Android Studio を起動
3. `File → Open` → `HKDLConqueror2026` フォルダを選択
4. "Trust Project" をクリック

### 3. Gradle Sync
- 自動的にGradle Syncが始まります（初回は2〜5分、依存関係のダウンロード）
- エラーが出た場合: `File → Sync Project with Gradle Files`

### 4. デバッグAPKのビルド
```
Build → Build Bundle(s) / APK(s) → Build APK(s)
```
または、ターミナルで:
```bash
./gradlew assembleDebug
```
APKの出力先: `app/build/outputs/apk/debug/app-debug.apk`

### 5. リリースAPKのビルド（署名あり）
```
Build → Generate Signed Bundle/APK → APK → 署名情報を入力 → Release
```

### 6. 実機インストール
```bash
adb install app/build/outputs/apk/debug/app-debug.apk
```
または Android Studio の実行ボタン（▶）で直接インストール

---

## 📁 ファイル構成

```
HKDLConqueror2026/
├── app/
│   ├── src/main/
│   │   ├── java/com/hkdl/conqueror2026/
│   │   │   ├── MainActivity.kt           # メインUI (Jetpack Compose)
│   │   │   ├── data/
│   │   │   │   └── AttractionData.kt     # 全17アトラクションデータ
│   │   │   ├── ui/
│   │   │   │   ├── screens/
│   │   │   │   │   └── RouteMapView.kt   # カスタムマップビュー
│   │   │   │   └── theme/
│   │   │   │       └── Theme.kt          # ディズニーテーマ配色
│   │   │   └── ...
│   │   ├── res/values/strings.xml
│   │   └── AndroidManifest.xml
│   └── build.gradle.kts
├── gradle/
│   ├── libs.versions.toml
│   └── wrapper/gradle-wrapper.properties
├── build.gradle.kts
└── settings.gradle.kts
```

---

## ✨ 機能一覧

| 機能 | 説明 |
|------|------|
| アトラクションリスト | 17か所を最適順序でカード表示 |
| チェックリスト | タップで完了/未完了トグル |
| プログレスバー | 制覇率をリアルタイム表示 |
| マップ画面 | エリア色分けピン＋ルートライン表示 |
| 詳細ダイアログ | タップで移動ナビ詳細を表示 |
| ディズニーテーマ | ネイビー×ゴールド×ホワイト配色 |

---

## 🎨 テーマ配色
- 背景: `#0D1B3E` (Disney Navy)
- アクセント: `#FFD700` (Disney Gold)
- テキスト: `#F8F4EC` (Disney White)
- 完了色: `#4CAF50` (Green)

---

## ⚠️ 注意事項
- GPS・位置情報は使用しません（パーミッション不要）
- インターネット接続不要でオフライン動作
- Android 8.0 (API 26) 以上が必要

---

*Generated by HKDL Conqueror 2026 Project Generator*
