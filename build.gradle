buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // 要求を下げることで、今の環境(8.5)でも動くようにします
        classpath 'com.android.tools.build:gradle:8.2.2'
        classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:1.8.10'
    }
}
allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
